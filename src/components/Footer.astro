---
import Monogram from './Monogram.astro';
import DarkModeToggle from './darkModeToggle.astro';
import BottomMenu from './BottomMenu.astro'
import Tooltip from './Tooltip.astro';
interface Props {
    className?: string;
}

const {className} = Astro.props;
---

<footer class={className}>
    <DarkModeToggle />
    <BottomMenu />
    
    <Tooltip tooltipArrow={true} tooltipText='Made with 🧗 in 🇨🇿' tooltipPosition={"left"} tooltipClassName='dark:bg-light dark:text-dark bg-dark text-light font-mono '><Monogram className='xl:w-24'/></Tooltip>
</footer>
